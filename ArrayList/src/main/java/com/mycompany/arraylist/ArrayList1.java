/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 */

package com.mycompany.arraylist;
import java.util.ArrayList;

/**
 *
 * @author JOAOPEDRODESBESELAPP
 */
public class ArrayList1 {
    
    public static void main(String[] args) {
        
          //ArrayList<Integer> numeros = new ArrayList<>(); // Criar um ArrayList
     // Adicionar elementos
        /*numeros.add(10);
        numeros.add(20);
        numeros.add(50);
        numeros.add(100);
        
         numeros.remove(2); // Remove o elemento no índice 2

        // Imprimir o ArrayList
        System.out.println(numeros);
        //Retorna o elemento na posição especificada ou Consulta o valor do indice.
        System.out.println(numeros.get(2));
        // Troca o valor de um elemento.
        numeros.set(0,100);
        System.out.println(numeros.get(0));
        // Acessa a primeira ocorrência de um valor ou Retorna o índice da primeira ocorrência de um elemento ou -1 se não estiver presente.
        System.out.println(numeros.indexOf(100));
        // Acessa a ultima ocorrência de um valor ou Retorna o índice da última ocorrência de um elemento ou -1 se não estiver presente.
        System.out.println(numeros.lastIndexOf(100));
        // Acessa um indice que não existe.
        System.out.println(numeros.indexOf(999));
        // Verifica o tamanho do ArrayList.
        System.out.println(numeros.size());
        // Verifica se o ArrayList esta vazio ou não.
        System.out.println(numeros.isEmpty());
        // Verifica se um elemento está na lista.
        System.out.println(numeros.contains(100));*/
        
        Shopping shopping = new Shopping();
        shopping.menu();
    }   
}
